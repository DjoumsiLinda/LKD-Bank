export default function Transfer(props) {
    return (
        <div className="transfer">
            <p>
                toto: transfer {props.first} {props.last} {props.iban}
            </p>
        </div>
    );
}
